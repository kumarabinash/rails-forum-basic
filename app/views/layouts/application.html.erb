<!DOCTYPE html>
<html>
<head>
  <title>Forum</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="nav">
<% if user_signed_in? %>
  <%= current_user.email %>
  <%= link_to "Sign Out", destroy_user_session_path, :method => 'delete' %>
<% else %>
  <%= link_to "Sign Up", new_user_session_path %>
<% end %>
</div>
<section class="row">
  <main class="col-9">
    <%= yield %>
  </main>
  <aside class="col-2">
  <h3>Topics</h3>
  </aside>
</section>



<% if !(notice == nil) %>
<script type="text/javascript">
  k$.growl({
    text: '<%= notice %>', // Optional
    delay: 2000, // Optional, 0 = forever
    type: 'alert-warn', // Optional
    // id: 'token' // Optional
  });
</script>
<% end %>

<%= "Previous url is - " %>
<%= debug session[:previous_url] %> <br />

<%= "request fullpath is " %>
<%= debug request.fullpath %>
</body>
</html>
