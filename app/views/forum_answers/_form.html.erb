<%= form_for @forum_answer do |f| %>
	<%= f.text_area :body, placeholder: "Write your response here..." %>
	<%= f.hidden_field :forum_question_id, value: @forum_question.id %>
	<%= f.submit %>
<% end %>